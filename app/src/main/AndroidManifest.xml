<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teachingtoddlers">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeachingToddlers">
        <activity android:name=".additionLevelThree"></activity>
        <activity android:name=".additionLevelTwo" />
        <activity android:name=".additionLevelOne" />
        <activity android:name=".alphabetGame" />
        <activity android:name=".countGame" />
        <activity android:name=".additionGame" />
        <activity android:name=".homePage" />
        <activity
            android:name=".newAdmin"
            android:parentActivityName=".accountCreation" />
        <activity
            android:name=".accountCreation"
            android:parentActivityName=".LoginPage" />
        <activity
            android:name=".forgotPassword"
            android:parentActivityName=".LoginPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>